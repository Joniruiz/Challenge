import React from 'react'
import { useSelector } from 'react-redux'


const Result = () => {

    const {result,keywords} = useSelector(state = state.reducer.search)

    return (
        <div>
            
        </div>
    )
}

export default Result
